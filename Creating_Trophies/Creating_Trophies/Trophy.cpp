#include "Trophy.h"
#include <string>
#include <iostream>

using namespace std;

Trophy::Trophy()
{
	m_name = "Default";
	m_level = 1;
	m_color = BRONZE;
}

Trophy::Trophy(string name, int level, Color color)
{
	//here is a comment

}